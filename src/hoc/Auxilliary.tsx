import React from 'react';
import { Props } from '../types/Props';
// import { Props } from '../types/Props';

const Aux = (props: React.PropsWithChildren<any>) => props.children;

export default Aux;
